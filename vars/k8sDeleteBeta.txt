## Deletes a HELM beta release

**Arguments:**

* **project**: the name of the project (e.g. `hello-go`).

**Requirements:**

* A node with HELM
* `[PROJECT]-build` Namespace
* Tiller deployed to the `[PROJECT]-build` Namespace
